# TSP problem truth

`objective.py` 与 `validate.py` 已实现独立Euclidean objective/constraints，handcrafted和真实reference测试LOCAL_VERIFIED。API/语义/边界见 [VALIDATION_DESIGN](../../docs/VALIDATION_DESIGN.md)。模型产生的实际解尚未验证；不因此标记integration完成。
